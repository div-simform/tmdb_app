import 'package:flutter/material.dart';

class MovieIconWidget extends StatelessWidget {
  const MovieIconWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.movie),
    );
  }
}
